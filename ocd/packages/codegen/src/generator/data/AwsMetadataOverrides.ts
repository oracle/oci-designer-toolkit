/*
** Terraform metadata overrides for the AWS provider. Empty by default — add
** resource-specific overrides here as AWS model coverage grows.
*/

export const terraformMetadataOverrides = {
    "common": {
    }
}
