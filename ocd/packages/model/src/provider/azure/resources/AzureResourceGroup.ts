/*
** Copyright (c) 2020, 2024, Oracle and/or its affiliates.
** Licensed under the Universal Permissive License v 1.0 as shown at https://oss.oracle.com/licenses/upl.
*/

import { OcdResources } from "../../../OcdDesign.js"
import * as AutoGenerated from "./generated/AzureResourceGroup.js"

export interface AzureResourceGroup extends AutoGenerated.AzureResourceGroup {}

export namespace AzureResourceGroup {
    
    export function newResource(type?: string): AzureResourceGroup {
        const resource = {
            ...AutoGenerated.AzureResourceGroup.newResource('resource_group'),
        }
        return resource
    }
    export function cloneResource(resource: AzureResourceGroup, type?: string): AzureResourceGroup {
        return AutoGenerated.AzureResourceGroup.cloneResource(resource, 'resource_group') as AzureResourceGroup
    }
    export function allowedParentTypes(): string[] {
        return []
    }
    export function getParentId(resource: AzureResourceGroup): string {
        const parentId = resource.compartmentId
        return parentId
    }
    export function setParentId(resource: AzureResourceGroup, parentId: string): AzureResourceGroup {
        return resource
    }
    export function getConnectionIds(resource: AzureResourceGroup, allResources: OcdResources): string[] {
        // This List of Ids does not include the Parent Id or Compartment Id
        let associationIds: string[] = []
        return associationIds
    }
}

export class AzureResourceGroupClient extends AutoGenerated.AzureResourceGroupClient {
    static new(): AzureResourceGroup {
        return AzureResourceGroup.newResource()
    }
    static clone(resource: AzureResourceGroup): AzureResourceGroup {
        return AzureResourceGroup.cloneResource(resource)
    }
}

export default AzureResourceGroupClient
