/*
** Copyright (c) 2020, 2024, Oracle and/or its affiliates.
** Licensed under the Universal Permissive License v 1.0 as shown at https://oss.oracle.com/licenses/upl.
*/

import * as AutoGenerated from "./generated/OciRemotePeeringConnection.js"
import { OciResource } from '@ocd/model'

export class OciRemotePeeringConnection extends AutoGenerated.OciRemotePeeringConnection {
    // The codegen gates `peer_tenancy_id` behind an `is_cross_tenancy_peering`
    // field that the RPC model does not have, so cross-tenancy peering exported
    // WITHOUT its peer tenancy — broken HCL. The real OCI provider has no such
    // argument; a present `peer_tenancy_id` IS the cross-tenancy signal. Emit it
    // whenever the field is set (matching how drg_id / peer_id are emitted).
    isAssignPeerTenancyId = (resource: Record<string, any>, _rootResource: OciResource): boolean =>
        this.isGenerateAttribute("peer_tenancy_id", resource.peerTenancyId, false)
}

export default OciRemotePeeringConnection
