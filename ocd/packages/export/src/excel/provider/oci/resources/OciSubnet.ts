/*
** Copyright (c) 2020, 2024, Oracle and/or its affiliates.
** Licensed under the Universal Permissive License v 1.0 as shown at https://oss.oracle.com/licenses/upl.
*/
    
import * as AutoGenerated from "./generated/OciSubnet.js"
import { OciModelResources as Model } from '@ocd/model'
import { ExcelColumnProperties } from '../../../OcdExcelResource.js'
import { TableColumnProperties } from 'exceljs'

export class OciSubnet extends AutoGenerated.OciSubnet {
    resourceColumns = (): ExcelColumnProperties[] => {
        const columns: ExcelColumnProperties[] = [
            {header: 'VCN', key: 'vcnId', width: 30},
            {header: 'Route Table', key: 'routeTableId', width: 30},
            {header: 'Security Lists', key: 'securityListIds', width: 30},
            {header: 'CIDR Block', key: 'cidrBlock', width: 50}
        ]
        return columns
    }
    resourceTableColumns = (): TableColumnProperties[] => {
        const columns: TableColumnProperties[] = [
            {name: 'VCN', filterButton: true},
            {name: 'Route Table', filterButton: true},
            {name: 'Security Lists', filterButton: true},
            {name: 'CIDR Block', filterButton: true}
        ]   
        return columns
    }
    resourceRowGeneration = (resource: Model.OciSubnet): any[] => [
        this.generateReferenceAttribute(resource.vcnId, this.allResources),
        this.generateReferenceAttribute(resource.routeTableId, this.allResources),
        this.generateReferenceListAttribute(resource.securityListIds, this.allResources),
        resource.cidrBlock
    ]
}

export default OciSubnet
